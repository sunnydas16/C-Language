#include<stdio.h>
int main(){
    
    char arr[5];
    
    printf("Enter the String :");
    scanf("%[^\n]", arr);

    // length of string logic
    int i;
    for(i=0;i<arr[i];i++);

    printf("Length of String is %d", i);

    return 0;
}